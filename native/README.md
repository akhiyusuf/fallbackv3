# native/

M7 owns this directory: iOS WidgetKit target sources and Android AppWidget
provider sources, injected into the generated `ios/` and `android/` projects by
`plugins/withFallbackWidgets.js`. See docs/ARCHITECTURE.md §9.
