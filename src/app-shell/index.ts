/**
 * M0. Boot sequence + provider composition consumed by app/_layout.tsx.
 * STUB — M0 implements.
 */
export declare function useAppBootstrap(): {
  ready: boolean;
  storeStatus: 'ready' | 'corrupt' | 'uninitialised';
};
